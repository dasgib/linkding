- content_for :head do
  = auto_discovery_link_tag(:atom, params.merge(format: 'atom'))

= form_tag bookmarks_path, method: :get, class: 'search_form' do
  = text_field_tag :q, params[:q]
  =# submit_tag "Go", name: nil

%h1= t('.title')

= paginated_section @bookmarks do
  = render @bookmarks

= link_to t('.new'), new_bookmark_path

- content_for :sidebar do
  = render 'tag_list'
