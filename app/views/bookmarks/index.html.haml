%h1= t('.title')

= paginated_section @bookmarks do
  = render @bookmarks

- content_for :sidebar do
  %h2 Tags
  %ul.tags
    - @tags.each do |tag|
      %li= link_to "#{tag.name} (#{tag.count})", bookmark_path(tag: tag.name)
